指针
一、基本介绍
1. 基本数据类型，变量存的就是值，也叫值类型
2. 获取变量的地址,用&，例如获取num的地址,&num
3. 指针类型，指针变量存的是一个地址，这个地址指向的空间存的才是值
    例如 var ptr *int = &num
4. 获取指针类型所指向的值，使用*
    例如 var ptr *int 使用 *ptr 获取ptr指向的值
    
二、理解
1. 基本数据类型的变量有值，有地址
2. 指针变量也有值和地址，指针变量的值是基本数据类型的地址
