一、init函数
1. 每一个源文件都可以包含一个init函数，该函数会在main函数执行前，
被Go运行框架调用，也就是说init会在main函数前被调用
2. 通常可以在init函数中完成初始化工作
3. 如果一个文件同时包含全局变量定义，init函数和main函数，则执行的流程是
全局变量定义 -> init函数 -> main函数
变量定义中 var age=test()，那么test()先执行
4. GOPATH方式做包管理， 引入包从src开始
5. main.go和utils.go都含有变量定义，init函数时
    被引入文件的变量定义[执行1]
    被引入文件的init函数[执行2]
    当前文件的变量定义[执行3]
    当前文件的init函数[执行4]
    当前文件的main函数[执行5]