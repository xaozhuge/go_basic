一、方法和函数的区别
1. 对于普通函数，接收者为值类型时，不能将指针类型的数据直接传递，反之亦然
2. 对于方法，接收者为值类型时，可以直接用指针类型的变量调用方法，反过来也同样可以
3. (&p).test03() 从形式上是传入地址，但是本质仍然是值拷贝，取决于方法绑定的类型
4. p.test04()，从形式上是传入值类型，但是本质上仍然是地址拷贝，取决于方法绑定的类型
5. 不管调用形式如何，真正决定是值拷贝还是地址拷贝，看这个方法是和哪个类型绑定
