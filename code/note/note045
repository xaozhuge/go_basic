一、字符串类型注意事项和使用细节
1. 字符串一旦赋值了,字符串就不能修改了,在Go中字符串是不可变的
    不能通过 str[0]="a"的方式去修改字符串的值
2. 字符串的表示形式
    双引号 会识别转义字符
    反引号 以字符串的原生形态输出,包括换行和特殊字符,可以实现防止攻击、输出源代码等效果
3. 字符串拼接方式
    使用 + 号
4. 当一行字符串太长时,需要使用多行字符串,+号留在行尾
